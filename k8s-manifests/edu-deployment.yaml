apiVersion: apps/v1
kind: Deployment
metadata:
  name: edu-doc-compare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edu-doc-compare
  template:
    metadata:
      labels:
        app: edu-doc-compare
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: edu-doc-compare
        image: docker.io/mikkagg/edu_doc_compare:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        volumeMounts:
        - name: study-plans-storage
          mountPath: /app/StudyPlans
        - name: chromadb-storage
          mountPath: /app/chromadb_store
      volumes:
      - name: study-plans-storage
        hostPath:
          path: /home/egor/Рабочий\ стол/my_project/StudyPlans
      - name: chromadb-storage
        hostPath:
          path: /home/egor/Рабочий\ стол/my_project/chromadb_store
